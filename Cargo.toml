
[workspace]
resolver = "2"
members = [
    "proto",
    "lib",
    "sim",
    "cli",
]

[patch.crates-io]
nam-ledger-proto = { path = "proto" }
nam-ledger-lib = { path = "lib" }
ledger-sim = { path = "sim" }
